import { Container } from '@mui/material';
import { Box } from '@mui/system';

export const Testimonial = () => {
  return (
    <Box>
      <Container maxWidth='xl'>Testimonial</Container>
    </Box>
  );
};
