import { Container } from '@mui/material';
import { Box } from '@mui/system';

export const SpecialtyList = () => {
  return (
    <Box>
      SpecialtyList
      <Container maxWidth='xl'>Tab</Container>
      <Container maxWidth='xl'>list</Container>
    </Box>
  );
};
